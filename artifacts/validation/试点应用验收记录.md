# Java 试点应用验收记录

本文档用于记录 Spring Boot 和 Tomcat 试点应用从源码仓库、构建、交付、部署到回滚的结果。试点源码样例位于：

- `examples/java-springboot`
- `examples/java-tomcat`

## Spring Boot

BuildSpec：

```text
build_strategy = java_springboot
source_path = examples/java-springboot
build_command = mvn clean package -DskipTests
artifact_path = target/paas-springboot-demo.jar
java_version = 17
runtime_base_image = registry.example/runtime/java17:1.0
```

验收项：

- [ ] 本地 `mvn clean package -DskipTests` 产出 jar。
- [ ] PaaS 创建 Application 成功。
- [ ] Jenkins 构建成功并生成镜像。
- [ ] Release、Freight 自动生成。
- [ ] 晋级 dev/test/staging/prod 成功。
- [ ] 回滚到历史 Freight 成功。

记录：

```text
2026-05-31 本地预检：当前执行环境未安装 Maven，`mvn -version` 返回 command not found，尚未执行 jar 打包验证。
```

## Tomcat

BuildSpec：

```text
build_strategy = java_tomcat
source_path = examples/java-tomcat
build_command = mvn clean package -DskipTests
artifact_path = target/paas-tomcat-demo.war
java_version = 17
runtime_base_image = registry.example/runtime/tomcat8:1.0
```

验收项：

- [ ] 本地 `mvn clean package -DskipTests` 产出 war。
- [ ] PaaS 创建 Application 成功。
- [ ] Jenkins 构建成功并生成镜像。
- [ ] Release、Freight 自动生成。
- [ ] 晋级 dev/test/staging/prod 成功。
- [ ] 回滚到历史 Freight 成功。

记录：

```text
2026-05-31 本地预检：当前执行环境未安装 Maven，`mvn -version` 返回 command not found，尚未执行 war 打包验证。
```

## 当前阻断

| 编号 | 问题 | 影响 | 建议处理 |
| --- | --- | --- | --- |
| pilot-001 | 当前执行环境未安装 Maven | 无法本地验证 Spring Boot jar 和 Tomcat war 产物 | 在构建节点或本机安装 Maven 3.9+ 后执行两个样例的 `mvn clean package -DskipTests` |
